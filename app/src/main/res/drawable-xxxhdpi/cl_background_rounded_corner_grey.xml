<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">

    <padding
        android:bottom="@dimen/spacing_12"
        android:left="@dimen/spacing_16"
        android:right="@dimen/spacing_16"
        android:top="@dimen/spacing_12"/>

    <!-- view background color -->
    <solid android:color="@android:color/transparent"/>

    <!-- view border width -->
    <stroke
        android:width="@dimen/spacing_1"
        android:color="@color/alto"/>

    <!-- Here is the corner radius -->
    <corners android:radius="@dimen/spacing_6"/>
</shape>